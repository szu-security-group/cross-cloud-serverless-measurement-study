# Benchmark 操作指南（速查版）

> **首次使用？** 请先阅读 [操作指南-详细版.md](操作指南-详细版.md)，按顺序完成环境准备 → 云平台配置 → Properties 填写。  
> 本文档适用于**已配置完环境**后的快速命令参考。

---

## 前提

- JAR 已构建：`target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar`
- 4 个 Properties 文件已配置：`Properties-tencent/ali/aws/azure` （详见 [详细版 §四](操作指南-详细版.md#四properties-配置文件填写)）
- 测试文件存在：`D:\testdata\testfile.txt`（~7MB）（详见 [详细版 §2.4](操作指南-详细版.md#24-准备测试文件)）
- 阿里云需要：`D:\tmp\jre11.tar.gz` （详见 [详细版 §3.2.5](操作指南-详细版.md#325-准备-jre-包)）
- 各云平台已开通服务并创建资源 （详见 [详细版 §三](操作指南-详细版.md#三云平台资源配置)）

---

## 命令行通用格式

```
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark <测试类型> [平台...]
```

> 以下命令均在项目根目录 `D:\作业\读论文\源代码2\IntegrityCheckingUsingSCFII\` 下执行。

---

## 第一步：构建 JAR

```bash
mvn package -Pdevelopment-Azure -DskipTests
```

如果 mvn 不在 PATH 中，用完整路径：

```bash
mvn package -Pdevelopment-Azure -DskipTests
```

---

## 第二步：测试模式验证（30秒快速检查）

部署前先验证代码能跑通。测试模式只测 512MB 单线程 3 次，不重新构建和部署：

```bash
java -DtestMode=true -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit Tencent
```

输出中看到 `VERIFY FAILED` 才算有问题，没有就说明正常。

---

## 第三步：部署函数代码

如果只是修改了 Handle 代码、需要把新 JAR 推上云端：

```bash
# 全部部署（5-10分钟）
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark deploy

# 只部署指定平台
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark deploy Tencent Ali
```

注意：`deploy` 命令内部会重新执行 `mvn package`，确保代码是最新的。

---

## 第四步：运行实验

### 审计测试 → 云审计各自.csv

全部平台：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit
```

逐平台（推荐分批跑，降低风险）：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit Tencent
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit Ali
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit AWS
```

参数：5 内存 × 5 线程 × 30 次 × 3 平台 = 2250 次调用，约 4-6 小时。

### 斐波那契测试 → FIV.csv

全部平台：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark fib
```

逐平台：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark fib Tencent
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark fib Ali
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark fib AWS
```

参数：5 内存 × 30 次 × 3 平台 = 450 次调用，约 20-40 分钟（Fast Doubling 算法）。

### S3 统一存储测试 → s3.csv

全部平台：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark s3
```

逐平台：
```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark s3 Tencent
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark s3 Ali
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark s3 AWS
```

参数：4 内存 × 30 次 × 3 平台 = 360 次调用，约 2-3 小时。

### Azure 专用测试 → azure_benchmark_data.csv

```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark azure
```

Azure 只有一个平台，无需逐平台。

参数：6 线程 × 10 次 = 60 次调用，约 30 分钟。

### 一键全部

```bash
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark all
```

---

## 产出文件

| 命令 | 输出文件 | 格式说明 |
|------|---------|---------|
| `audit` | `云审计各自.csv` | `CSP,Memory_MB,Thread,Run_ID,Execution_Time_ms` |
| `fib` | `FIV.csv` | `CSP,Memory_MB,Run_ID,Execution_Time_ms` |
| `s3` | `s3.csv` | `CSP,Memory_MB,Run_ID,Execution_Time_ms,Total_Time_ms` |
| `azure` | `azure_benchmark_data.csv` | `threads,test_id,exec_time,mem_usage` |

文件在项目根目录生成。如果文件已存在，新数据会**追加**到末尾（不会覆盖）。

---

## 修改参数

编辑 `test-config.xml`，不用改代码：

```xml
<test type="audit" enabled="true">
    <memorySizes>128,256,512,1024,2048</memorySizes>
    <threadCounts>1,2,4,8,16</threadCounts>
    <repetitions>30</repetitions>
</test>
```

设置 `enabled="false"` 可跳过某项测试。改完直接生效，无需重新构建。

---

## 运行时间总览

| 测试 | 调用次数 | 预计耗时 |
|------|---------|---------|
| audit（三平台） | 2,250 | 4-6 小时 |
| fib（三平台） | 450 | 20-40 分钟 |
| s3（三平台） | 360 | 2-3 小时 |
| azure | 60 | 30 分钟 |
| **全部** | **~3,120** | **5-12 小时** |

---

## 常见问题

**函数处于 Updating 状态**：内存更新会自动重试（最多 10 次，间隔 10 秒）。持续失败的话等 1-2 分钟再跑。

**VERIFY FAILED**：说明审计证明验证不通过。检查云端数据是否被覆盖（重新 KeyGen+OutSource 可解决）。

**AWS 128MB**：audit 测试大部分调用 Failed（内存不足，函数超时），fib 测试不受影响可正常运行。程序不跳过此配置，Failed 由云函数执行失败产生。

**阿里云冷启动慢**：当前代码已将 JRE 打包进部署包（`createAliZip`），首次冷启动解压到 `/tmp/jre`（约 3 秒），后续热启动直接复用，无需额外处理。此问题已在代码层面解决。

**测试中断了怎么办**：CSV 是追加模式，已写入的数据不会丢失。重新跑同一个测试类型会从第一个参数重新开始（已有行的参数会被重复测试，建议手动清理 CSV 再重跑对应平台）。

---

## 参考资料

- **首次环境搭建、云平台配置、Properties 填写** → [操作指南-详细版.md](操作指南-详细版.md)
- **架构说明、审计协议、设计决策** → [README.md](README.md)
- **已知 Bug、修复记录、数据对比** → [问题记录.md](问题记录.md)
