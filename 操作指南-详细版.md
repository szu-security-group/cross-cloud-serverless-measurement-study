# 操作指南 — 详细完整版

本指南面向**没有接触过本项目**的读者，从零开始搭建环境、配置云平台、运行实验、获取实验数据。

---

## 一、项目概述

本项目测量不同云平台（腾讯云 / 阿里云 / AWS / Azure）上无服务器云函数（SCF）的性能，包含四种测试：

| 测试类型 | 说明 | 输出文件 |
|---------|------|---------|
| audit | 云存储完整性审计（I/O密集，多线程下载+RS纠删码证明计算） | 云审计各自.csv |
| fib | 斐波那契计算（CPU密集，纯计算无下载） | FIV.csv |
| s3 | 统一存储审计（三平台从同一个 AWS S3 桶下载） | s3.csv |
| azure | Azure 专用格式测试 | azure_benchmark_data.csv |

### 审计协议（5步）

```
KeyGen → OutSource → Upload → Audit(挑战) → Prove(SCF内执行) → Verify
```

参数：RS编码 n=255, k=223, GF(2^8)，挑战长度 l=460，测试文件 ~7MB。

---

## 二、环境准备

### 2.1 安装 JDK

本项目使用 **JDK 1.8**（推荐 Amazon Corretto 8 或 Oracle JDK 8）。

```bash
java -version
# 应输出类似：java version "1.8.0_xxx"
```

### 2.2 安装 Maven

下载 [Apache Maven 3.9+](https://maven.apache.org/download.cgi)，解压到本地目录（如 `C:\Users\<用户名>\tools\apache-maven-3.9.9`）。

验证安装：

```bash
mvn --version
# 应输出 Maven 版本和使用的 JDK 信息
```

记下 Maven 可执行文件的**完整路径**，后续需填入 Properties 配置：
- Windows: `C:/Users/<用户名>/tools/apache-maven-3.9.9/bin/mvn.cmd`
- Linux/Mac: `/usr/local/bin/mvn`

### 2.3 获取代码

```bash
git clone <仓库地址>
cd IntegrityCheckingUsingSCFII
```

项目根目录结构（需确认以下文件存在）：

```
├── pom.xml                  # Maven 项目配置
├── test-config.xml           # 测试参数配置
├── Properties-tencent        # 腾讯云配置模板
├── Properties-ali            # 阿里云配置模板
├── Properties-aws            # AWS 配置模板
├── Properties-azure          # Azure 配置模板
├── src/main/java/            # Java 源代码
└── mdPics/                   # 架构图
```

### 2.4 准备测试文件

创建一个约 **7MB** 的文本文件，作为完整性审计的测试数据：

```bash
# Windows PowerShell
$size = 7475448; $bytes = new-object byte[] $size; (new-object Random).NextBytes($bytes); [IO.File]::WriteAllBytes("D:\testdata\testfile.txt", $bytes)

# Linux/Mac
dd if=/dev/urandom of=/root/testdata/testfile.txt bs=1024 count=7301
```

文件路径记下，填入 Properties 的 `filePath` 字段。

### 2.5 创建临时目录

```bash
mkdir D:\tmp    # Windows
mkdir /tmp      # Linux/Mac（通常已存在）
```

此目录用于存放 KeyGen 生成的中间文件。阿里云平台还需要在此目录放置 JRE 包（详见 §3.2.5）。

---

## 三、云平台资源配置

四个云平台需要**分别开通服务并创建资源**。建议按顺序逐个配置，每完成一个平台就运行一次测试验证。

### 通用准备

在各自云平台控制台**创建 API 密钥**（AccessKey），用于 SDK 调用：

| 平台 | 密钥管理页面 | 需要获取 |
|------|------------|---------|
| 腾讯云 | CAM → 访问密钥 → API密钥管理 | SecretId + SecretKey |
| 阿里云 | RAM → 用户 → 创建AccessKey | AccessKey ID + AccessKey Secret |
| AWS | IAM → 用户 → 安全凭证 → 创建访问密钥 | Access Key ID + Secret Access Key |
| Azure | 存储账户 → 访问密钥 | 连接字符串 |

> **建议**：为密钥分配最小权限（仅限对象存储读写 + 云函数调用/更新），不要使用主账号密钥。

---

### 3.1 腾讯云

#### 3.1.1 开通服务

- **对象存储 COS**：https://console.cloud.tencent.com/cos5
- **云函数 SCF**：https://console.cloud.tencent.com/scf

#### 3.1.2 创建 COS 存储桶

1. 进入 COS 控制台 → 存储桶列表 → 创建存储桶
2. 名称如 `tpds-in-scf-test-tokyo-<APPID>`（APPID 可在账号信息中查看）
3. 区域选择：**ap-tokyo**（东京），与论文实验保持一致
4. 访问权限：**私有读写**
5. 记下存储桶名称和所属区域

#### 3.1.3 创建 SCF 云函数

1. 进入 SCF 控制台 → 函数服务 → 新建
2. 函数名称：`audit-test-tokyo`
3. 运行环境：**Java 8**
4. 函数类型：**Event 函数**
5. 其余默认，先创建空函数（代码由 Benchmark 自动上传）
6. 记下函数名称

#### 3.1.4 获取并配置密钥

在 CAM 控制台获取 SecretId 和 SecretKey，填入 `Properties-tencent` 文件（详见 §四）。

---

### 3.2 阿里云

#### 3.2.1 开通服务

- **对象存储 OSS**：https://oss.console.aliyun.com
- **函数计算 FC**：https://fc.console.aliyun.com

#### 3.2.2 创建 OSS 存储桶

1. 进入 OSS 控制台 → Bucket 列表 → 创建 Bucket
2. 名称：`scftestbucket`
3. 区域：**ap-northeast-1**（东京）
4. 读写权限：**私有**
5. 记下 Bucket 名称和所属区域

#### 3.2.3 创建 FC 函数

1. 进入 FC 控制台 → 函数 → 创建函数
2. 函数名称：`audit-test-ali-tokyo`
3. 运行环境：**custom.debian10**
4. 其余默认，先创建空函数
5. 记下函数名称

#### 3.2.4 获取账号 ID

在阿里云控制台右上角头像处查看 **Account ID**（用于构造 OSS endpoint）。

#### 3.2.5 准备 JRE 包

阿里云 FC 使用 custom runtime，需要自行提供 JDK。本项目已实现 JRE 自动捆绑：

1. 下载 **Amazon Corretto 11** 的 Linux x64 压缩包
2. 将压缩包重命名为 `jre11.tar.gz` 并放置在 `D:\tmp\`（或 `/tmp/`）
3. Benchmark 部署时会把 JRE 打包进函数 zip，首次冷启动自动解压到 `/tmp/jre`

> 如果不需要阿里云测试，可跳过此步骤。

#### 3.2.6 获取密钥

在 RAM 控制台获取 AccessKey ID 和 AccessKey Secret。

---

### 3.3 AWS

#### 3.3.1 开通服务

- **S3**：https://s3.console.aws.amazon.com
- **Lambda**：https://console.aws.amazon.com/lambda

#### 3.3.2 创建 S3 存储桶

1. 进入 S3 控制台 → 创建存储桶
2. 名称：`scftestbucket-aws`
3. 区域：**ap-northeast-1**（东京）
4. 阻止所有公开访问：**开启**
5. 记下存储桶名称

#### 3.3.3 创建 IAM 角色

Lambda 函数需要 IAM 角色来访问 S3：

1. 进入 IAM 控制台 → 角色 → 创建角色
2. 信任实体：**Lambda**
3. 附加策略：`AmazonS3FullAccess`（或限制到特定桶）
4. 角色名称：`scf-audit-lambda-role`
5. 记下角色名称

#### 3.3.4 创建 Lambda 函数

1. 进入 Lambda 控制台 → 创建函数
2. 函数名称：`audit-test-aws`
3. 运行环境：**Java 11**
4. 执行角色：选择刚创建的 `scf-audit-lambda-role`
5. 记下函数名称
6. 记下 **AWS 账号 ID**（用于 S3 权限）

#### 3.3.5 获取密钥

在 IAM 控制台创建 Access Key，获取 Access Key ID 和 Secret Access Key。

---

### 3.4 Azure

#### 3.4.1 开通服务

- **Blob Storage**（存储账户）：https://portal.azure.com
- **Function App**：https://portal.azure.com

#### 3.4.2 创建存储账户

1. 进入 Azure Portal → 存储账户 → 创建
2. 名称：`scftestazure`
3. 区域：**Japan West**
4. 性能：**标准**
5. 记下存储账户名称

#### 3.4.3 获取连接字符串

1. 进入存储账户 → 访问密钥
2. 复制 **连接字符串**（Connection string），格式如：
   ```
   DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
   ```

#### 3.4.4 创建 Function App

1. 进入 Azure Portal → Function App → 创建
2. 名称：`audit-test-azure`
3. 发布：**代码**
4. 运行环境：**Java 11**
5. 操作系统：**Windows**
6. 区域：**Japan West**
7. 计划类型：**消费计划（Consumption Plan）**
8. 记下 Function App 名称

#### 3.4.5 创建 Blob 容器

1. 进入存储账户 → 容器 → 创建
2. 名称：`scf-audit-container`

#### 3.4.6 获取函数 URL

部署后，进入 Function App → 函数 → Audit → 获取函数 URL，填入 `Properties-azure` 的 `functionUrl`。

---

## 四、Properties 配置文件填写

四个 `Properties-{platform}` 文件各有相同的通用字段和平台特有字段。Benchmark 运行时会自动将对应平台的文件复制为 `Properties`。

### 4.1 通用字段

| 字段 | 说明 | 示例值 |
|------|------|--------|
| `mavenExecutable` | Maven 可执行文件的路径（PATH中则填 `mvn`） | `mvn` |
| `filePath` | 测试文件（~7MB）的完整路径 | `D:/testdata/testfile.txt` |
| `tmpFilePath` | 临时文件目录 | `D:/tmp` |
| `memorySize` | 初始内存（MB），部署时用 | `512` |
| `timeout` | 函数超时时间（秒） | `900` |
| `blockNum` | COS 分块上传的并行块数（腾讯云专用） | `4` |
| `partNum` | 多线程上传的分片数 | `4` |

### 4.2 平台特有字段

| 字段 | Tencent | Ali | AWS | Azure |
|------|---------|-----|-----|-------|
| `secretId` | 腾讯云 SecretId | 阿里云 AccessKey ID | AWS Access Key ID | Azure 存储连接字符串 |
| `secretKey` | 腾讯云 SecretKey | 阿里云 AccessKey Secret | AWS Secret Access Key | `placeholder`（Azure 不用） |
| `regionName` | `ap-tokyo` | `ap-northeast-1` | `ap-northeast-1` | `japanwest` |
| `bucketName` | COS 桶名（含 APPID） | OSS Bucket 名 | S3 Bucket 名 | Blob 容器名 |
| `functionName` | SCF 函数名 | FC 函数名 | Lambda 函数名 | Function App 名 |
| `handler` | `com...TenHandle::mainHandler` | `com...AliHandle::handleRequest` | `com...AwsHandle::handleRequest` | `Audit` |
| `runtime` | `Java8` | `custom.debian10` | `java11` | `Java11` |
| `aliAccountId` | — | 阿里云 Account ID | — | — |
| `awsAccountId` | — | — | AWS Account ID | — |
| `roleName` | — | — | IAM 角色名 | — |
| `functionUrl` | — | — | — | Function App URL |

### 4.3 填写示例（腾讯云）

```properties
mavenExecutable=C:/Users/zhangsan/tools/apache-maven-3.9.9/bin/mvn.cmd
filePath=D:/testdata/testfile.txt
tmpFilePath=D:/tmp
memorySize=512
timeout=900
blockNum=4
partNum=4

secretId=AKIDxxxxxxxxxxxxxxxxxxxxxxxxxxxx
secretKey=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
regionName=ap-tokyo
bucketName=tpds-in-scf-test-tokyo-1415612789
functionName=audit-test-tokyo
handler=com.fchen_group.TPDSInScf.Run.TenHandle::mainHandler
runtime=Java8
```

> **注意**：四份 Properties 文件的通用字段（前7行）保持一致，只有平台特有字段不同。

---

## 五、构建与运行

所有命令在项目根目录（`IntegrityCheckingUsingSCFII/`）下执行。

### 5.1 Maven 构建

```bash
mvn package -Pdevelopment-Azure -DskipTests
```

如果 `mvn` 不在 PATH 中，使用完整路径：

```bash
C:/Users/<用户名>/tools/apache-maven-3.9.9/bin/mvn.cmd package -Pdevelopment-Azure -DskipTests
```

**参数说明**：
- `-Pdevelopment-Azure`：激活 development-Azure profile，打包所有四个平台的 SDK 依赖
- `-DskipTests`：跳过单元测试

构建成功后，JAR 位于 `target/TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar`。

### 5.2 测试模式验证（30 秒）

部署**之前**先做快速验证，确保代码逻辑正确。测试模式只测 512MB 单线程 3 次，不重新构建和部署：

```bash
java -DtestMode=true -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit Tencent
```

观察输出：
- 出现 `VERIFY FAILED`：审计协议有问题，检查云端数据是否被覆盖
- 正常完成无 VERIFY FAILED：代码逻辑正确，可以继续

### 5.3 部署函数代码

部署会把最新 JAR 上传到各云平台并更新云函数代码：

```bash
# 部署所有四平台（5-10分钟）
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark deploy

# 部署指定平台
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark deploy Tencent Ali
```

> 部署命令内部会触发 `mvn package`，确保上传的是最新代码。首次部署耗时较长（各平台 SDK 上传代码 + 创建函数）。

### 5.4 运行审计测试 → 云审计各自.csv

为各平台的原生对象存储执行完整性审计。参数：5 内存 × 5 线程 × 30 次重复。

```bash
# 全部平台（2250次调用，约 4-6 小时）
java -cp target\TPDSInSCF-1.0-SNAPSHOT_Benchmark-jar-with-dependencies.jar com.fchen_group.TPDSInScf.Run.Benchmark audit

# 逐平台（推荐，降低意外中断风险）
java -cp ... Benchmark audit Tencent
java -cp ... Benchmark audit Ali
java -cp ... Benchmark audit AWS
```

> Azure 不支持 audit 测试（Consumption Plan 无内存配置 API）。

### 5.5 运行斐波那契测试 → FIV.csv

在云函数内计算 fib(800,000)（167,190位数字），测量纯 CPU 性能。参数：5 内存 × 30 次重复。

```bash
# 全部平台（450次调用，约 20-40 分钟）
java -cp ... Benchmark fib

# 逐平台
java -cp ... Benchmark fib Tencent
java -cp ... Benchmark fib Ali
java -cp ... Benchmark fib AWS
```

### 5.6 运行 S3 统一存储测试 → s3.csv

三平台从**同一个 AWS S3 桶**下载数据，比较跨云下载性能。参数：4 内存 × 30 次重复。

```bash
# 全部平台（360次调用，约 2-3 小时）
java -cp ... Benchmark s3

# 逐平台
java -cp ... Benchmark s3 Tencent
java -cp ... Benchmark s3 Ali
java -cp ... Benchmark s3 AWS
```

### 5.7 运行 Azure 专用测试 → azure_benchmark_data.csv

Azure 格式独立，测试多线程下载加速。参数：6 线程 × 10 次重复。

```bash
java -cp ... Benchmark azure
```

### 5.8 一键运行全部

```bash
java -cp ... Benchmark all
```

执行顺序：audit → fib → s3 → azure。**总耗时约 5-12 小时**，建议分批运行。

---

## 六、修改测试参数

编辑 `test-config.xml` 可自定义测试参数，无需修改代码：

```xml
<test type="audit" enabled="true">
    <csvFile>云审计各自.csv</csvFile>
    <platforms>Tencent,Ali,AWS</platforms>
    <memorySizes>128,256,512,1024,2048</memorySizes>
    <threadCounts>1,2,4,8,16</threadCounts>
    <repetitions>30</repetitions>
</test>
```

| 参数 | 说明 |
|------|------|
| `enabled` | `true`=运行，`false`=跳过此项测试 |
| `platforms` | 逗号分隔的平台列表 |
| `memorySizes` | 逗号分隔的内存配置（MB） |
| `threadCounts` | 逗号分隔的线程数（仅 audit/azure 测试有效） |
| `repetitions` | 每个配置的重复次数 |
| `fibN` | 斐波那契计算的 n 值（默认 800,000） |

> 修改后直接运行 Benchmark 即可生效，无需重新构建。

---

## 七、输出数据说明

### 7.1 文件清单

| 测试 | 输出文件 | 格式 |
|------|---------|------|
| audit | `云审计各自.csv` | `CSP,Memory_MB,Thread,Run_ID,Execution_Time_ms` |
| fib | `FIV.csv` | `CSP,Memory_MB,Run_ID,Execution_Time_ms` |
| s3 | `s3.csv` | `CSP,Memory_MB,Run_ID,Execution_Time_ms,Total_Time_ms` |
| azure | `azure_benchmark_data.csv` | `threads,test_id,exec_time,mem_usage` |

### 7.2 字段说明

- **Execution_Time_ms**：云函数内部的执行时间（下载 + 证明计算），纳秒转毫秒
- **Total_Time_ms**：客户端从发起到收到响应的总时间（仅 s3 测试有此列）
- **Run_ID**：重复编号（1~30），论文中 30 次 = 5 次/时段 × 6 个时间点
- **Thread**：格式为 "1 Thread"、"2 Threads" 等
- 部分调用可能为 `Failed`（超时或函数执行失败），属正常现象

### 7.3 数据追加机制

CSV 文件以**追加模式**写入。如果中途中断，已写入的数据不会丢失。重新运行同一测试会从第一个参数重新开始，因此已有参数的重复数据会再次追加。如需重跑，建议先删除已有 CSV。

### 7.4 分批运行策略（推荐）

为避免长时间运行中意外中断丢失进度，建议按平台分批：

```bash
# 先跑腾讯云
java -cp ... Benchmark audit Tencent
java -cp ... Benchmark fib Tencent
# 再跑阿里云
java -cp ... Benchmark audit Ali
java -cp ... Benchmark fib Ali
# ... 以此类推
```

---

## 八、常见问题与排查

### 8.1 函数处于 Updating 状态

内存更新后函数进入 `Updating` 状态，程序会自动重试（最多 10 次，间隔 10 秒）。持续失败的话等待 1-2 分钟后再运行。

### 8.2 VERIFY FAILED

审计证明验证不通过。可能原因：
1. 云端数据被覆盖或损坏 → 删除云端文件，重新运行让 Benchmark 自动 KeyGen+OutSource+Upload
2. 参数不一致 → 检查 `test-config.xml` 中的 `DATA_SHARDS=223, PARITY_SHARDS=32` 是否与代码一致

### 8.3 AWS 128MB 失败

AWS Lambda 128MB 配置下 Java 函数经常超时（JVM 内存不足）。这是预期行为，程序不会跳过此配置，失败的调用 CSV 中会标记为 `Failed`。

### 8.4 阿里云冷启动慢

代码已将 JRE 打包进部署包（`createAliZip`），首次冷启动解压到 `/tmp/jre`（约 3 秒），后续热启动直接复用。如果冷启动超过 60 秒，检查 `D:\tmp\jre11.tar.gz` 是否存在。

### 8.5 CSV 文件被覆盖

早期版本存在此 Bug（`writeCsvHeader` 用 `new FileWriter(csvPath, false)`），现已修复。如果使用旧版代码，每次运行会清空已有数据。确保构建的是最新代码。

### 8.6 AWS 内存更新 ResourceConflictException

AWS 函数代码部署后立即更新内存会触发冲突。代码已内置 60 秒等待 + 扩展重试条件（匹配 "in progress" 和 "ResourceConflictException"）。

### 8.7 Azure Function URL

Azure 部署后如果函数 URL 发生变化，需更新 `Properties-azure` 中的 `functionUrl` 字段。可在 Azure Portal → Function App → 函数 → 获取函数 URL 中找到。

---

## 九、运行时间参考

| 测试 | 调用次数 | 预计耗时 |
|------|---------|---------|
| audit（单平台） | 750 次 | 1.5-2 小时 |
| audit（三平台） | 2,250 次 | 4-6 小时 |
| fib（三平台） | 450 次 | 20-40 分钟 |
| s3（三平台） | 360 次 | 2-3 小时 |
| azure | 60 次 | 30 分钟 |
| **全部** | **~3,120 次** | **5-12 小时** |

---

## 参考资料

- 项目 README：`README.md` — 架构说明与 API 文档
- 问题记录：`问题记录.md` — 已知 Bug 及修复记录、与参考数据对比
- 速查版指南：`操作指南.md` — 已配置环境后的快速命令参考
